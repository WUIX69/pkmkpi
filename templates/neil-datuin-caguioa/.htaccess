RewriteEngine On

# Pigilan ang pag-browse sa loob ng mga folders (Security)
Options -Indexes

# Itago ang .php sa URL
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME}\.php -f
RewriteRule ^([^\.]+)/?$ $1.php [NC,L]